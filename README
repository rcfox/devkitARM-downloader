Downloads the latest DevKit ARM toolchain and extracts it properly.

To run this, you need:
   Config::INI::Reader
   Archive::Tar
   Parallel::ForkManager

One argument can be given: The number of forks to have concurrently.
